const config = {
    backendUrl: "http://nhl-team-info-backend-env.eba-ffnrunat.us-east-1.elasticbeanstalk.com",
  };
  
  console.log(`backendUrl in config.js: ${config.backendUrl}`)
  export {config as urlConfig}